# URL du serveur PocketBase, uniquement si le frontend n'est PAS servi par
# PocketBase lui-même (sinon l'app utilise automatiquement la même origine).
# VITE_PB_URL=http://serveur-cirad.example.org:8090
